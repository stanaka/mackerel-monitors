machine:
  ruby:
    version: 2.2.2
dependencies:
  pre:
    - curl -fsSL https://mackerel.io/assets/files/scripts/setup-apt.sh | sudo sh
    - sudo apt-get update -qq
    - sudo apt-get install mkr
test:
  override:
    - rake
